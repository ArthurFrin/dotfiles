{
  "layer": "top",
  "position": "top",
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["hyprland/window"],
  "modules-right": ["pulseaudio", "network", "battery", "clock", "tray"],

  "hyprland/workspaces": { "all-outputs": true, "format": "{icon}" },
  "hyprland/window": { "max-length": 60, "separate-outputs": false },

  "pulseaudio": { "format": "{volume}% {icon}", "tooltip": false },
  "network": { "format-wifi": "{essid} {signalStrength}%", "format-ethernet": "eth {ifname}", "format-disconnected": "offline" },
  "battery": { "format": "{capacity}% {icon}", "states": { "warning": 25, "critical": 10 } },
  "clock": { "format": "{:%a %d %b  %H:%M}" },
  "tray": { "spacing": 6 }
}

